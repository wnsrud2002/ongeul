# mdmaker 개발 가이드라인

작성일: 2026-09-20 · 상태: 1~5단계 구현 완료(v0.1.1, 2026-09-21) · 실행 환경: 개인 PC, 로컬 처리

> 이 문서는 구현 전에 쓴 설계 기준이다. 실제 지원 범위·옵션·검증 결과는 README가 기준이다.

## 1. 목적과 성공 기준

엑셀·한글·워드·PDF 등 문서 파일을 AI에 전달하기 전에 로컬에서 Markdown으로 변환한다. 문서의 전체 내용과 구조를 미리 추출해 재사용하는 프로그램이다. 최우선 요구사항은 내용의 완전 보존이며, 토큰 절감이나 처리 속도 때문에 이를 양보하지 않는다.

- 외부 API, 클라우드 변환 서비스, LLM 호출 없이 변환한다.
- 원문에 없는 요약·해석·문장을 생성하지 않는다.
- 본문, 제목, 목록, 표, 수식, 이미지, 주석, 각주, 숨김 데이터와 출처 위치를 빠짐없이 보존한다.
- 단일 파일과 폴더 일괄 변환을 지원한다.
- 요약·축약·생략·자동 삭제·중복 내용 제거를 금지한다. 원문에 반복된 내용도 그대로 보존한다.
- 처리하지 못했거나 보존 여부를 확인하지 못한 내용이 있으면 변환 완료로 처리하지 않는다. 빈 결과나 경고만 붙인 불완전한 결과도 성공이 아니다.
- 원본 파일은 수정하지 않는다.

Markdown은 제목·목록·표 등을 일반 텍스트로 표현하는 문서 형식이다.

### 완전 보존의 의미와 출력 한계

문장의 일부, 숫자, 단위, 부호, 수식, 셀 관계, 이미지, 주석을 삭제하거나 바꾸지 않는다. 취소선·색상·들여쓰기처럼 의미를 전달하는 서식과 원래 순서도 보존 대상이다. 인코딩 변경과 Markdown 이스케이프는 허용하지만, 해석한 내용이 원문과 동일해야 한다.

단일 `.md` 파일만으로 모든 문서 형식의 이미지·병합 셀·임베디드 개체·레이아웃을 손실 없이 표현할 수는 없다. 따라서 Markdown 본문과 함께 필요한 원본 자산·구조 정보를 보존하는 출력 묶음을 설계한다. 자산은 원본 바이트로 저장하고, 구조 정보에는 위치·순서·서식·관계를 기록해 본문에서 참조한다. 이는 요약을 허용한다는 뜻이 아니다.

원본 파일을 그대로 두거나 첨부했다는 이유만으로 내용 변환까지 성공했다고 판정하지 않는다. 변환 결과에서 모든 보존 대상이 대응·검증되어야 한다. 단일 `.md`만으로 보존할 수 없는 입력은 그 제약을 명확히 보고한다. 이미지 링크나 보조 파일을 AI에 전달하지 않으면 AI가 전체 내용을 받은 것도 아니다.

지원 형식 안에서도 입력마다 보존 검사를 수행한다. 자동 검사가 불가능한 요소나 OCR 결과는 `unverified`로 남기며, 검사 가능한 범위를 넘어 100% 보존을 보장한다고 표시하지 않는다.

### 토큰 절감에 대한 전제

변환 자체에는 AI 토큰이 들지 않지만, 결과 Markdown을 AI에 입력하면 그 텍스트에 대한 토큰은 발생한다. 원문과 같은 내용을 전부 보내면 토큰이 크게 줄지 않을 수 있으며, 표 마크업 때문에 늘어날 수도 있다. 첨부 문서를 처리하는 방식도 AI 서비스마다 다르다.

따라서 목표를 특정 절감률로 고정하지 않는다. 반복 변환 방지와 전체 내용을 유지하는 분할로 처리 비용과 한 번에 전달하는 양을 관리한다. 모든 조각을 전달하면 총 토큰이 줄지 않을 수 있다. 내용·서식 삭제나 페이지·시트·구역 제외로 절감률을 만들지 않는다. 파일 용량 감소율을 토큰 감소율로 보고하지 않는다.

## 2. 개발 범위

“모든 문서”는 장기적인 확장 목표다. 초기부터 모든 확장자·버전·레이아웃을 완벽하게 지원한다고 표시하지 않는다. 지원 여부는 실제 샘플로 확인하고, 형식별로 `지원`, `제한 지원`, `미지원`을 구분한다.

| 형식 | 구현 단계 | 기본 변환 경로 | 주요 한계 |
| --- | --- | --- | --- |
| `.txt`, `.md`, `.csv`, `.tsv` | 1단계 | Python 표준 라이브러리 | 인코딩, 구분자, Markdown 특수문자 |
| `.docx` | 1단계 | MarkItDown 검증 후 사용 | 복잡한 표, 텍스트 상자, 변경 추적 |
| `.xlsx` | 1단계 | MarkItDown 검증, 필요 시 openpyxl 보완 | 수식 캐시, 표시 형식, 병합 셀 |
| `.hwpx` | 2단계, 핵심 범위 | ZIP/XML 구조를 읽는 전용 변환 함수 | 중첩 표, 각주, 서식 참조 |
| `.hwp` | 2단계, 핵심 범위 | HWP 5.x용 로컬 파서 검증 | 구버전, 암호화·배포용 파일, 표 구조 |
| 텍스트 PDF, `.pptx` | 3단계 | MarkItDown 검증 후 사용 | 읽기 순서, 도표, 슬라이드 배치 |
| 스캔 PDF, `.png`, `.jpg`, `.tiff` | 4단계 | 로컬 렌더러와 Tesseract OCR | 인식 오류, 손글씨, 복잡한 표 |
| `.doc`, `.xls`, `.ppt`, `.odt`, `.ods`, `.odp`, `.rtf` | 5단계 | 검증된 로컬 변환기 또는 LibreOffice 중간 변환 | 중간 변환에 따른 손실 |
| `.html`, `.epub` 등 | 이후 확장 | 기존 변환기 재사용부터 검토 | 외부 리소스 차단, 읽기 순서 |

한글 지원은 부가 기능으로 미루지 않는다. 1단계만 완료한 상태에서는 “엑셀·워드 초기 버전”으로 표시하고, HWP/HWPX 검증까지 마쳐야 핵심 요구사항을 충족한 것으로 본다. HWP 3.x 등 구버전은 별도 지원 대상으로 남긴다.

암호 해제, 손상 복구, 이미지 속 도표 의미 해석, 원본과 동일한 인쇄 레이아웃 재현은 초기 범위에 포함하지 않는다. 해당 파일이나 요소를 만나면 이유와 위치를 알려준다. 필요한 내용을 보존·검증할 수 없으면 `unsupported`, `partial` 또는 `unverified`로 남기며 완전 변환으로 제공하지 않는다.

## 3. 기술 선택 원칙

### 기본 구성

- 언어: Python 3.12를 초기 검증 기준으로 삼는다. 실제 의존성 호환성을 확인한 뒤 지원 버전을 고정한다.
- 사용자 인터페이스: 명령줄 프로그램부터 구현한다. GUI는 변환 품질 검증 이후에 추가한다.
- 기본 기능: `argparse`, `pathlib`, `csv`, `json`, `hashlib`, `tempfile`, `subprocess`, `zipfile`, `xml.etree.ElementTree` 등 표준 라이브러리를 우선한다.
- 범용 변환 후보: MarkItDown. 공식적으로 Word·Excel·PowerPoint·PDF 등의 변환과 형식별 선택 의존성을 제공한다. HWP/HWPX 지원은 별도 경로로 검증한다. [공식 README](https://github.com/microsoft/markitdown/blob/main/README.md)
- OCR 후보: Tesseract. OCR은 이미지 속 글자를 텍스트로 읽는 기술이며, 엔진과 언어 데이터가 로컬에 설치되어 있어야 한다. [공식 설치 문서](https://tesseract-ocr.github.io/tessdoc/Installation.html)

이 문서의 라이브러리 이름은 도입 후보이며, 설치 승인이나 설치 완료를 뜻하지 않는다. 구현 시 기존 의존성을 먼저 확인하고, 신규 의존성이 필요하면 이유와 대안을 설명한 뒤 사용자 승인을 받는다. 현재 가이드 작성 단계에서는 아무 패키지도 설치하지 않는다.

### 범용 변환기를 먼저 검증하는 이유

처음부터 모든 파일 형식의 파서를 직접 만들지 않는다. MarkItDown으로 대표 파일을 변환한 뒤 아래 보존 규칙을 만족하는지 확인한다. 통과하면 그대로 재사용하고, 실패한 요구사항에 대해서만 전용 함수를 추가한다. 내부의 비공개 함수에 의존하는 패치는 피한다.

예를 들어 엑셀의 수식·저장값·숨김 데이터 보존이 부족하면 엑셀 경로만 openpyxl로 보완한다. 직접 사용하는 라이브러리는 다른 패키지의 간접 의존성에 기대지 말고 프로젝트 의존성으로 명시한다. 범용 변환기가 Markdown을 반환했다는 사실만으로 보존 검증을 대신하지 않는다.

### 외부 API 없이 실행하는 조건

1. 입력은 로컬 파일만 허용한다. URL 입력은 거부한다.
2. 범용 변환기의 로컬 전용 진입점을 사용하고, 클라우드 엔드포인트·LLM 클라이언트·자동 플러그인을 연결하지 않는다.
3. 문서 내부의 링크·외부 이미지·외부 참조 데이터를 가져오지 않는다. 참조 주소와 파일에 저장된 값은 보존하되, 실제 외부 내용이 필요하고 파일에 없으면 완전 보존 확인 불가로 표시한다.
4. OCR 엔진과 언어 데이터는 사전 설치한다. 실행 중 모델 다운로드나 업데이트를 하지 않는다.
5. 통계 전송, 오류 보고 업로드, 원격 로그 수집을 넣지 않는다.
6. 네트워크를 차단한 환경에서 변환 테스트를 통과해야 오프라인 지원으로 표시한다.

패키지 최초 설치에 인터넷을 쓰는 것과 변환 중 외부 API를 쓰는 것은 별개다. 인터넷이 없는 PC에도 설치해야 한다면, 검증된 패키지·실행 파일·언어 데이터를 미리 준비하는 배포 절차를 추가한다.

## 4. 처리 흐름과 최소 구조

입력 수집 → 파일 형식·크기 검사 → 원본 요소 목록 작성 → 전체 내용·자산·구조 추출 → Markdown과 보조 파일 임시 저장 → 원본과 보존 대조 → 검증 상태에 따른 결과 저장 순서로 처리한다. 내용 정리·축약 단계는 두지 않는다.

초기 구현은 `mdmaker.py`와 작은 `test_mdmaker.py`부터 시작한다. 형식별 함수가 커질 때만 한글·엑셀·OCR 처리 코드를 파일로 분리한다. 웹 서버, 데이터베이스, 작업 큐, 플러그인 프레임워크는 초기 구현에 필요하지 않다.

형식별 변환 함수는 공통으로 다음 정보를 반환한다.

| 항목 | 내용 |
| --- | --- |
| Markdown | 추출한 문서 내용 |
| 상태 | `success`, `partial`, `unverified`, `failed`, `unsupported`, `skipped` |
| 경고 | 누락·추정·지원하지 않는 요소와 원본 위치 |
| 보존 검사 | 원본 요소와 결과 위치의 대응, 불일치·미검증 항목 |
| 보조 파일 | 원본 자산, Markdown으로 표현하지 못하는 구조·서식 정보 |
| 출처 | 입력 파일명, 페이지·시트·구역 등의 확인 가능한 위치 |
| 처리 정보 | 변환기 이름·버전, 처리 시간, 문자 수 |

처음에는 사전이나 간단한 데이터 구조 하나로 충분하다. 복잡한 공통 문서 모델을 먼저 설계하지 않는다. 긴 입력은 페이지·시트·행 묶음 단위로 처리하고, 모든 파일을 동시에 메모리에 올리지 않는다.

## 5. 형식별 구현 규칙

### 5.1 워드 DOCX

- 본문 문단과 표를 문서에 등장하는 순서대로 출력한다.
- 제목 스타일은 Markdown 제목으로, 목록은 순서와 들여쓰기를 살려 변환한다.
- 각주, 하이퍼링크, 머리글·바닥글, 주석, 텍스트 상자, 변경 추적을 보존하고 입력별로 확인한다. 누락하면 `partial`이며, 검증하지 못하면 `unverified`다.
- 페이지는 글꼴·렌더링에 따라 달라지므로, 확정할 수 없는 DOCX 페이지 번호를 만들어 붙이지 않는다. 대신 제목이나 문단 위치를 쓴다.
- 변경 추적의 삽입·삭제 내용과 상태를 모두 구별해 보존한다. 자동 수락·거절하지 않는다. 이를 지원하지 못하는 경로에서는 완전 변환 성공을 반환하지 않는다.
- 표의 헤더가 확실하지 않으면 첫 행을 임의로 헤더로 바꾸지 않는다.

### 5.2 엑셀 XLSX

- 시트마다 제목을 붙인다. 모든 시트·행·열을 포함하고 숨김 상태도 보존한다. 일부 시트·범위만 변환하는 옵션은 두지 않는다.
- 저장된 빈 셀·서식·빈 행·열의 좌표와 범위를 보존한다. 긴 빈 영역은 동일 구조로 복원할 수 있는 범위 정보로 기록할 수 있지만, 의미가 없다고 판단해 삭제하지 않는다.
- 셀의 `0`, `False`, 빈 문자열, 빈 셀, 오류 값(`#DIV/0!` 등)을 구분한다.
- 수식과 저장된 계산값을 모두 구별해 보존한다. 캐시가 없으면 수식과 `[원본에 계산값 없음]`을 출력한다. 원본에 없는 값을 생성하지 않으며 저장된 값도 최신이라는 보장은 없음을 기록한다.
- openpyxl은 수식을 계산하지 않는다. `data_only=True`는 저장된 계산값을 읽는 옵션이다. 필요 시 수식용·값용 읽기를 대응시킨다. [수식 문서](https://openpyxl.readthedocs.io/en/stable/simple_formulae.html), [읽기 옵션](https://openpyxl.readthedocs.io/en/stable/tutorial.html)
- 원본의 외부 링크나 매크로를 실행해 값을 채우지 않는다. 자동 재계산도 기본 동작에 넣지 않는다.
- 병합 셀은 좌상단 값을 보존하고 병합 범위를 덧붙인다. 주변 셀에 값을 자동 복제하면 합계나 의미가 달라질 수 있으므로 하지 않는다.
- 날짜, 퍼센트, 통화, 앞자리 0이 있는 코드의 원시 값과 표시 형식을 모두 보존한다. 표시 형식을 재현·검증하지 못하면 `unverified`로 남긴다.
- 작은 표는 Markdown 표로, 큰 표는 Markdown 안의 TSV 코드 블록으로 출력하는 선택지를 제공한다. 원래 행 번호와 셀 범위를 남긴다.
- 최대 행 번호가 비정상적으로 큰 파일을 고려해 행·셀 처리 한도를 둔다. 한도에 도달하면 조용히 자르지 않고 `partial`로 표시한다.

### 5.3 한글 HWPX

HWPX는 ZIP 안에 XML과 첨부 데이터를 담는 형식이다. 단순히 모든 텍스트 태그를 이어 붙이면 표와 본문 관계를 잃으므로 구조를 읽어야 한다. [한컴 공식 구조 설명](https://tech.hancom.com/hwpxformat/)

- ZIP 내부의 형식 정보와 필수 항목을 확인한다.
- `Contents/content.hpf`의 문서 순서를 기준으로 구역을 읽는다. 파일명 문자열 정렬만으로 읽기 순서를 결정하지 않는다.
- XML 네임스페이스를 처리하고 문단, 텍스트 조각, 명시적 탭·줄바꿈을 구분한다.
- `header.xml`의 스타일 참조와 본문을 연결해 확인 가능한 제목 구조만 복원한다.
- 표 안의 문단을 본문 순회에서 다시 출력하지 않도록 한다. 표는 행·열·병합 범위를 읽어 변환한다.
- 각주·미주·그림·중첩 표도 보존한다. 미지원 요소를 발견하면 위치와 경고를 남기고 완전 변환 성공을 반환하지 않는다.
- `Preview`의 미리보기 텍스트를 전체 본문으로 취급하지 않는다.

### 5.4 한글 HWP

HWP는 HWPX와 다른 바이너리 형식이다. 확장자만 바꾸거나 ZIP 파서에 전달해 처리할 수 없다.

초기 후보는 pyhwp의 HWP 5.x 추출 기능이다. `hwp5txt`는 텍스트 추출 경로이며, 표 구조까지 보존하는 완전한 Markdown 변환기로 간주하지 않는다. 공식 문서는 변환 기능을 실험적으로 분류하고 있고, 소스에는 AGPL-3.0-or-later가 명시되어 있으므로 배포 방식에 맞는 검토를 거친다. [공식 문서](https://pyhwp.readthedocs.io/en/latest/), [라이선스 표기](https://github.com/mete0r/pyhwp/blob/master/src/hwp5/hwp5txt.py)

1. 실제 한글 샘플로 본문·표·한자·특수문자 추출을 먼저 검증한다.
2. Python 버전과 운영체제 호환성을 확인한다.
3. 텍스트만 추출하고 표·개체·구조를 잃는 경로는 조사용으로만 사용한다. 보존 검사를 통과하지 못하면 완전 변환 경로로 채택하지 않는다.
4. 표 구조가 핵심 요구를 만족하지 못하면, 구조를 제공하는 다른 로컬 파서나 HWPX 중간 변환 경로를 비교한다. 신규 런타임 도입은 별도 승인 후 진행한다.
5. 암호화·배포용·미지원 버전은 식별 가능한 범위에서 구분해 오류를 반환한다. 보호 기능을 우회하지 않는다.

한컴도 HWP/HWPX 라이브러리별 지원 범위와 표·이미지 추출 한계를 구분해 설명한다. 이를 바탕으로 지원 수준을 정하되, 실제 채택 여부는 자체 샘플 테스트로 결정한다. [한컴의 파서 비교](https://blog.hancom.com/en/hwp-hwpx-data-extraction-format-library/)

### 5.5 PDF·스캔·이미지

- 텍스트가 있는 PDF는 텍스트 추출을 먼저 수행하고 페이지 번호를 남긴다.
- 텍스트 부족 여부를 페이지별로 판단한다. 글자가 조금 있다는 이유만으로 이미지의 텍스트도 추출됐다고 간주하지 않는다.
- OCR은 옵션으로 켠다. 스캔·혼합 페이지의 의심 영역을 경고하고 사용자가 지정한 페이지를 강제로 OCR할 수 있게 한다.
- 스캔 PDF는 별도로 검증한 로컬 PDF 렌더러로 페이지 이미지로 만든 뒤 OCR한다. 렌더러 설치·라이선스·버전도 관리 대상이다.
- 한국어·영어 OCR은 해당 언어 데이터를 준비하고 Tesseract의 언어 선택 기능을 사용한다. [공식 실행 문서](https://github.com/tesseract-ocr/tessdoc/blob/main/Command-Line-Usage.md)
- OCR 결과에 원본 페이지와 OCR 사용 여부를 표시하고 원본 이미지도 보존한다. 숫자·고유명사·표 등에 인식 오류가 있을 수 있으므로 OCR 텍스트는 검수 전 `unverified`다. 원본 이미지 보관이나 높은 인식 신뢰도만으로 텍스트 정확성을 확정하지 않는다.
- 기존 텍스트와 OCR 결과를 페이지 단위로 무조건 합쳐 중복 출력하지 않는다. 영역 분리가 어렵다면 선택한 추출 방식과 한계를 기록한다.
- 다단 편집, 표, 수식, 차트의 완전 복원을 자동으로 보장할 수 없으면 성공 처리하지 않는다. PDF는 글자의 위치를 보존해도 문단·표 의미를 제공하지 않을 수 있다. [PDF 텍스트 추출의 한계](https://pypdf.readthedocs.io/en/stable/user/extract-text.html)
- 그림 원본과 기존 대체 텍스트를 함께 보존한다. 대체 텍스트가 없으면 `[이미지: 내용 해석 안 됨]`을 덧붙일 수 있으나, 이 문구로 이미지 자체를 대체하지 않는다. 그림의 의미를 임의로 설명하지 않는다.

### 5.6 구형 Office와 기타 문서

기존 변환기로 충분한지 먼저 확인한다. 필요하면 LibreOffice를 화면 없이 실행하는 변환 모드로 중간 파일을 만들고 그 파일을 Markdown으로 변환한다. `.doc` → `.docx`, `.ppt` → `.pptx` 같은 경로는 샘플로 검증한 조합만 지원한다. [LibreOffice 명령줄 문서](https://help.libreoffice.org/latest/en-GB/text/shared/guide/start_parameters.html)

독립 임시 작업 폴더와 프로필을 사용하고, 매크로 실행·외부 링크 갱신을 차단한다. 프로세스 종료 코드뿐 아니라 결과 파일의 존재·크기·파싱 가능 여부를 검사한다. 중간 변환 전후의 내용·구조·개체를 대조할 수 없으면 `unverified`이며, 차이가 있으면 `partial`이다. LibreOffice만으로 HWP 전체를 지원한다고 가정하지 않는다.

## 6. Markdown 출력 규칙

- UTF-8과 LF 줄바꿈을 사용한다.
- 첫머리에 원본 파일명과 형식을 적는다. 절대 경로·사용자명 등 불필요한 개인정보는 기본으로 넣지 않는다.
- 원문 제목 체계와 문단 순서를 보존한다. 근거 없이 글자 크기만 보고 제목으로 단정하지 않는다.
- 단순 표는 Markdown 표로, 복잡한 병합 표는 원본 좌표·병합 범위를 포함한 행별 표현으로 출력하고 구조 변환 사실을 표시한다.
- 셀 안의 `|`, 줄바꿈, 백틱 등을 안전하게 표현한다. 코드 블록 구분자는 본문과 충돌하지 않게 선택한다.
- 숫자, 단위, 부호, 소수점, 통화, 날짜를 임의로 요약하거나 반올림하지 않는다.
- 반복 공백·빈 줄·반복 문구를 자동 삭제하지 않는다. 자동 맞춤법 교정이나 중복 제거도 하지 않는다. Markdown에서 표현이 달라지는 공백·서식은 보조 구조 정보로 정확하게 보존한다.
- 누락·미검증 상태는 Markdown에도 표시한다. 경고를 남겼다는 이유로 불완전한 결과를 성공으로 승격하지 않는다.
- 이미지가 있으면 자산 보존은 필수다. 문서별 자산 폴더와 상대 링크를 사용하며 원본 바이트를 유지한다. 이미지 링크만으로 AI에 이미지 내용이 전달되지는 않는다는 점을 안내한다.
- 첨부 파일·임베디드 개체·수식 객체도 원본 데이터와 연결 위치를 보존한다. 실행은 하지 않는다. 데이터 보관과 내용 해석·검증 여부는 구분해 기록한다.

### 결과 파일 배치

입력 루트에 대한 상대 디렉터리 구조를 유지하고 원본 확장자를 파일명에 포함한다.

| 입력 | 출력 예시 |
| --- | --- |
| `input/계약서.docx` | `output/계약서.docx.md` |
| `input/계약서.hwp` | `output/계약서.hwp.md` |
| `input/재무/매출.xlsx` | `output/재무/매출.xlsx.md` |

자산·구조 정보가 필요하면 결과 Markdown 옆에 문서별 보조 폴더를 함께 생성한다. 본문과 보조 파일의 대응 목록도 저장한다. 이 파일들은 완전 보존 요구에 필요한 변환 산출물이다.

기존 결과는 기본적으로 덮어쓰지 않고 충돌을 보고한다. 명시적 덮어쓰기 옵션이 있을 때만 교체한다. 문서별 출력 묶음을 임시 위치에 준비하고 검증한 뒤 완료 처리해 중간 실패로 기존 결과를 훼손하지 않는다. 불완전·미검증 결과는 완료 결과와 구별되는 위치에 상태 표시와 함께 보관한다. 덮어쓰기 금지 상태에서는 동시 실행으로 생긴 충돌도 검사한다.

## 7. 사용 인터페이스

아래 명령은 모두 구현되어 있다. 전체 옵션은 README의 "쓰는 법"을 따른다.

```sh
python mdmaker.py "입력/계약서.docx" --out "출력"
python mdmaker.py "입력폴더" --out "출력폴더" --recursive
python mdmaker.py "매출.xlsx" --out "출력"
python mdmaker.py "보고서.pdf" --out "출력"
python mdmaker.py "스캔.pdf" --out "출력" --ocr auto --ocr-lang "kor+eng"
python mdmaker.py "긴문서.docx" --out "출력" --split-chars 12000
```

1단계에서는 입력 경로, `--out`, `--recursive`, `--overwrite`만 구현했고, 이후 단계에서 OCR·분할·재사용·PDF 옵션을 더했다. 문서 내 시트·페이지·구역 제외 옵션은 제공하지 않는다. OCR 대상 지정은 추출 방법 선택이며 나머지 페이지 생략을 뜻하지 않는다. 파일 형식에 맞지 않는 옵션은 무시하지 말고 오류로 알린다.

출력 폴더가 입력 폴더 안에 있어도 다시 읽지 않는다. 심볼릭 링크는 기본적으로 따라가지 않으며, 지원하는 일반 파일만 정렬된 순서로 수집한다. 한 파일이 실패해도 나머지는 계속 처리한다.

종료 코드는 `0=모든 입력의 전체 보존 검증 통과`, `1=부분 변환·미검증·실패·미지원·출력 충돌 있음`, `2=잘못된 인자 또는 실행 불가`로 정의한다. 이전에 전체 보존 검증을 통과했고 결과 묶음의 무결성을 다시 확인한 재사용 결과만 성공으로 센다.

## 8. 토큰과 처리 시간 줄이기

### 먼저 구현할 것

- 원문 전체를 유지한다. 빈 셀·서식·연속 빈 줄도 중요도를 추정해 제거하지 않는다.
- 모든 시트·페이지·구역을 변환한다. 분할하더라도 전체 조각이 원문 전체를 빠짐없이 포함해야 한다.
- 큰 표는 행 단위로 나누고 각 조각에 제목·열 이름·원본 행 범위를 반복해 문맥을 유지한다.
- 긴 문서는 제목·문단 경계에서 나눈다. 너무 큰 단일 문단·셀은 분할 표시를 붙이고 원래 위치를 남긴다.
- `--split-chars`는 문자 수 기준이다. 토큰 수와 동일하다고 표시하지 않는다.
- 분할 전후를 재결합해 내용·순서가 같은지 검사한다. 문맥용으로 추가한 제목·헤더는 원문 데이터와 구별해 중복 합산하지 않는다. 조각의 수·순서·원본 범위를 목록에 남긴다.

### 반복 사용 시 추가할 것

같은 문서를 자주 변환하게 되면 결과 폴더에 작은 JSON 기록을 두고 재사용한다. 파일 내용의 SHA-256, 모든 변환 옵션, 프로그램·변환기·보존 검사 버전이 같고 이전 상태가 `success`이며 Markdown·자산·구조 정보가 모두 온전한 경우에만 건너뛴다. 파일명이나 수정 시간만으로 동일성을 판단하지 않는다. 민감한 본문은 기록에 넣지 않는다.

단일 순차 처리부터 시작한다. 실제 병목을 측정한 다음 파일 단위 동시 처리를 제한적으로 추가한다. OCR은 CPU·메모리 비용이 커서 일반 문서와 같은 동시 처리 수를 적용하지 않는다.

## 9. 안전성과 오류 처리

- 확장자뿐 아니라 파일 시그니처와 내부 구조를 확인한다. 시그니처는 파일 내용에 있는 형식 식별 정보다.
- 파일 크기, ZIP 항목 수·해제 후 크기, XML 크기·깊이, 페이지 수, 셀 수, 이미지 픽셀 수, 처리 시간에 상한을 둔다. 기본값은 대표 샘플을 측정해 정하고 사용자가 조절할 수 있게 한다.
- ZIP 항목을 임의 경로에 풀지 않는다. 상대 경로 탈출, 절대 경로, 중첩 압축 폭탄을 차단한다.
- XML의 외부 엔티티·DTD·XInclude를 허용하지 않는다. 엔티티 확장과 자원 고갈 입력을 테스트하고, 표준 파서와 제한만으로 충분하지 않으면 보호용 파서 도입 이유를 설명한다.
- 외부 실행 파일은 인자 목록과 `shell=False`로 호출하고 시간 제한을 둔다. 시간 초과 시 관련 자식 프로세스도 정리한다.
- 변환기는 제한된 작업 디렉터리와 최소 권한으로 실행한다. 임시 파일은 성공·실패 모두 정리한다.
- 로그에는 상태·상대 파일명·오류 종류만 기록하고 원문 전체·비밀번호·토큰을 남기지 않는다.
- 내용 전체 추출 실패는 `failed`, 확인된 누락·변형은 `partial`, 보존 확인 불가는 `unverified`, 미지원 형식은 `unsupported`로 구분한다. 모두 완료 기준 미달이다. 크기·시간·셀 수 상한은 작업을 중단하는 안전장치이며, 내용을 잘라 성공시키는 기준이 아니다.
- 원문에 담긴 명령문도 문서 데이터로 처리한다. 변환기가 실행하지 않으며, Markdown 변환이 AI의 프롬프트 주입을 제거해 주는 것은 아니다.

## 10. 개발 순서와 완료 조건

| 단계 | 작업 | 완료 조건 |
| --- | --- | --- |
| 0. 샘플 검증 | 엑셀·워드·한글 대표 파일로 변환 후보 비교 | 원본 요소 목록과 보존 대조 방법 확정 |
| 1. 기본 CLI | TXT/CSV/DOCX/XLSX, 일괄 변환, 안전 저장 | 전체 내용·자산·구조 검사, 미달 결과 성공 금지 |
| 2. 한글 지원 | HWPX 구조 추출, HWP 5.x 경로 검증 | 한글 원문 전체와 구조 대조, 불가능한 요소 명시 |
| 3. 추가 문서 | PDF/PPTX 전체 변환 | 페이지·슬라이드 전체 대응, 읽기 순서 확인 |
| 4. OCR | 로컬 엔진, 언어 데이터, PDF 렌더러 연결 | 오프라인 실행, 원본 이미지 보존, 검수 전 미검증 상태 유지 |
| 5. 편의 기능 | 분할, 재사용, 구형 문서, 필요 시 GUI | 핵심 형식의 품질 저하 없이 추가 기능 검증 |

각 단계는 작동하는 최소 결과부터 완성한다. 검증하지 않은 형식을 지원 목록에 올리거나, HWP의 표 손실을 숨긴 채 전체 지원으로 발표하지 않는다.

## 11. 테스트와 품질 기준

초기에는 Python 표준 `unittest`로 작은 테스트 파일을 둔다. 테스트용 문서는 직접 만든 비민감 샘플을 사용한다. 실제 사용자 문서를 저장소에 넣지 않는다.

```sh
python -m unittest discover -s . -p 'test_*.py'
```

위 명령은 구현 후 테스트 실행 기준이며, 이 가이드만 있는 현재 폴더에서 테스트 완료를 의미하지 않는다.

필수 검증 항목:

- 한국어·영어·한자·특수문자·긴 파일명·공백 포함 경로.
- CSV 셀 안의 구분자·줄바꿈·따옴표, UTF-8 BOM과 CP949 입력. 인코딩 추정 실패 시 명시적 인코딩 옵션을 요구하고 문자를 조용히 버리지 않는다.
- DOCX의 본문 → 표 → 본문 순서, 목록, 하이퍼링크, 미지원 요소 경고.
- XLSX의 여러 시트, 숨김 데이터, 병합 셀, `0`·빈칸 구분, 수식 캐시 없음, 날짜·백분율·앞자리 0.
- HWP/HWPX의 다중 구역, 표 안 문단 중복, 각주, 미리보기와 본문 구별.
- PDF의 텍스트·스캔·혼합 페이지, OCR 중복, 빈 결과 처리.
- 손상·암호화·미지원 파일, 제한 초과, 출력 이름 충돌, 출력 폴더 재탐색 방지.
- 기존 결과 보호, 변환 중단 후 임시 파일 정리, 일괄 작업 중 한 파일 실패.
- 네트워크 차단 상태에서 모든 지원 경로 실행. 외부 리소스가 포함된 문서도 업로드·조회 없이 처리.

고정된 샘플의 일부 핵심 문구만 검사하지 않는다. 기대되는 전체 텍스트·숫자·단위·수식·표 관계·숨김 데이터·자산·순서를 비교한다. 원본 요소 목록은 변환 결과만으로 역생성하지 말고, 원본 구조를 검사해 작성하고 수작업으로 확인한 샘플과 대조한다. 같은 추출기의 누락을 동일한 코드로 재확인하는 검사는 보존 증명이 아니다.

입력마다 원본 요소와 출력 위치의 대응을 확인한다. 개수 일치만으로 통과시키지 말고 텍스트·값·관계와 자산 바이트의 해시를 검사한다. 해시는 파일 내용의 동일성을 확인하는 값이며 OCR 정확성이나 문서 의미까지 증명하지 않는다. 정의하지 않은 요소를 발견하거나 검사 범위를 확정할 수 없으면 `unverified`다.

누락·변형·미검증 요소를 의도적으로 포함한 샘플에서 `success`와 종료 코드 0이 나오지 않는지 검증한다. 원본만 복사해 놓은 경우, OCR 오류, 자산 유실, 분할 조각 누락·순서 변경, 수식 손실, 반복 문구 삭제도 실패 검증에 포함한다. 필요하면 사람이 원본과 결과 전체를 검수하고 그 범위를 기록한다.

성능 측정 시 파일 크기·페이지 수·셀 수·문자 수·소요 시간·최대 메모리·OCR 여부·운영체제·라이브러리 버전을 함께 기록한다. 디지털 문서와 OCR 결과는 분리해서 비교한다.

실제 토큰 수를 측정하려면 대상 AI 모델에 맞는 로컬 토크나이저를 선택한다. 토크나이저는 텍스트를 모델의 처리 단위로 나누는 도구다. 도입 승인 전에는 문자 수만 표시하며, 추정치를 확정 비용으로 표현하지 않는다. 숫자·조건·표 관계를 잃어 얻은 입력량 감소는 품질 개선으로 인정하지 않는다.

## 12. 구현 시 지켜야 할 작업 규칙

- 여러 파일을 변경하기 전에 변경 계획을 제시한다.
- 기존 코드·설치된 라이브러리·표준 기능을 먼저 재사용한다.
- 신규 의존성과 외부 실행 파일은 필요성을 설명하고 승인 후 추가한다.
- 주석은 구현 내용의 반복보다 선택 이유와 실제 한계를 설명한다.
- 비밀번호·API 키·토큰·사용자 원문을 코드나 커밋에 넣지 않는다.
- 변경 후 관련 테스트를 실행하고 명령과 결과를 제시한다.
- 요청받지 않은 서버, 로그인, DB, AI 요약, 벡터 검색 기능을 먼저 만들지 않는다.

이 가이드의 초기 구현 방향은 “기존 로컬 변환기 검증·재사용 + 전체 내용과 자산·구조 보존 + 보존 확인을 통과한 결과만 완료 처리”다. 토큰 감소보다 내용 완전 보존이 항상 우선한다.
